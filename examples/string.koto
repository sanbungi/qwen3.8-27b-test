関数 長（s: 文字列） -> なし {
    もし s == "あ" ならば {
        表示 1。
    } そうでなければ {
        表示 "それ以外"。
    }
}

主処理 {
    変数 s: 文字列 を "ことのは" とする。
    もし s == "ことのは" ならば {
        表示 "一致"。
    }
    もし s != "ことのは" ならば {
        表示 "不一致"。
    }
    長（s）。
}
